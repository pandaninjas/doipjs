"use strict";
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (Object.hasOwnProperty.call(mod, k)) result[k] = mod[k];
    result["default"] = mod;
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
var path = __importStar(require("path"));
var constants_1 = require("../constants");
var config_parser_1 = require("../lib/config-parser");
var file_finder_1 = require("../lib/file-finder");
var license_manager_1 = require("../lib/license-manager");
function addExports(exports, command) {
    for (var key in command) {
        /* istanbul ignore else  */
        if (command.hasOwnProperty(key)) {
            exports[key] = command[key];
        }
    }
}
exports.addExports = addExports;
function manageLicense(args, mode) {
    var config = config_parser_1.configParser(path.resolve(process.cwd(), args[constants_1.CONFIG_OPTION]));
    var paths = file_finder_1.getPaths(config.ignore, config.ignoreDefaultIgnores);
    var licenseManager = new license_manager_1.LicenseManager(paths, config.license, config.licenseFormats, config.defaultFormat, config.trailingWhitespace, mode, config.output);
    licenseManager.manage();
}
exports.manageLicense = manageLicense;
//# sourceMappingURL=utils.js.map